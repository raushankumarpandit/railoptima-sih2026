# RAILOPTIMA Frontend

React + Vite + TypeScript frontend for the SIH26027 railway block-planning prototype.

The frontend calls the FastAPI backend at `http://localhost:8000` by default. The backend in this build is backed by the supplied synthetic Delhi–Ghaziabad dataset.

Run:

```powershell
npm install
npm run dev
```
